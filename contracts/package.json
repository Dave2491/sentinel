{
  "name": "sentinel-contracts",
  "private": true,
  "version": "0.0.0",
  "description": "Minimal Sentinel treasury policy contracts for hackathon prototyping.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:mantle-sepolia": "hardhat run scripts/deploy.ts --network mantleSepolia",
    "verify:mantle-sepolia": "hardhat run scripts/verify.ts --network mantleSepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "dotenv": "^17.2.3",
    "hardhat": "^2.28.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0"
  }
}
